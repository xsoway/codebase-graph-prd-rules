id: edge-incomplete-input
title: "模块图谱业务规则: 术语不完整"
input:
  prompt: "请根据项目代码解释一个叫优先包的功能，但没有模块路径、代码符号或需求说明。"
expect:
  must_contain: ["信息缺口", "待确认", "候选"]
judge:
  type: rule_based
  success:
    - output_contains:
        all: ["信息缺口", "待确认"]

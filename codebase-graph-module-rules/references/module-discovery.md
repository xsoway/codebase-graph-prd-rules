# 模块图谱检索规则

先将用户词拆为：业务名、领域对象、动作、状态、外部系统、可能的表/字段。用这些词查询 Graphify；再从候选入口向服务、实体、Mapper、任务和客户端做路径查询。

最小完整链路通常覆盖：同步入口或消息/任务 → 编排服务 → 规则/比较器 → 持久化或缓存 → 日志/状态 → 外部调用。任何一个节点缺失时，要写为信息缺口，不以名称猜测。

若同一词命中多个模块，按调用路径和领域实体确定边界；如仍无法区分，列出候选范围并请求或记录待确认，而非混合多个模块的规则。

# 业务规则文档与测试契约

每个模块合并为一个章节，并至少包含：目的与角色、触发/输入、业务决策、输出/副作用、规则表、异常分支、数据/系统链接、来源、测试点。

规则表建议列：规则 ID、条件、决策、结果、例外、来源、测试断言。对状态型模块补状态图；对多判断分支补流程图。测试矩阵至少覆盖 P0 主链路、边界值、错误/回滚、状态迁移、并发或幂等（适用时）、数据准备、外部桩和未执行边界。

来源引用采用项目相对路径加行号，例如 `src/main/.../Service.java:120`。行号用于追溯，不能替代阅读上下文。

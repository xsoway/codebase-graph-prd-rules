id: edge-incomplete-input
title: "全项目图谱业务规则: 信息不完整"
input:
  prompt: "请根据代码生成业务规则，但没有给出项目路径和源码范围。"
expect:
  must_contain: ["信息缺口", "待确认", "假设"]
judge:
  type: rule_based
  success:
    - output_contains:
        all: ["信息缺口", "待确认"]
